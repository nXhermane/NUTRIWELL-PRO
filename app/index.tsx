import { Text, View } from "react-native";

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Hello I'm Hermane new app.Okay so the nutri well is the same app with nutriXnap</Text>
    </View>
  );
}
